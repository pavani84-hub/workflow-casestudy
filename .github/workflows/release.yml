name: Monthly Release
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Release
        run: echo "Deploying version $(date +'%Y-%m') to production"
